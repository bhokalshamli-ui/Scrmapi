services:
  - type: web
    name: scrmapi
    env: node                 # ← CRITICAL: Forces Node.js (NO Docker)
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
